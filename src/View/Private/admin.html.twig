{% extends 'layout.html.twig' %}

{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/Private/admin.css">
    {% endblock %}

    {% block content %}
        <a href="/">
            retour
        </a>
        <br>
            <h1 class="text-center mb-5">
                Admin Board
            </h1>
            <div class="container">
                <div class="row">
                    <div class="col-lg-5">
                        <p class="bg-white mb-1 p-1 rounded">
                            Quand tu comprend que tu va utiliser le MVC pour ton projet 2
                        </p>
                        <img id="image" src="http://lusile17.l.u.pic.centerblog.net/o/ca4f5ca4.jpg" class="img-fluid w-100" alt="your image"></div>

                        <div class="col-lg-7">
                            <p class="text-white mt-3 text-center">
                                Administration des légendes

                            </p>
                            {% set messages = [
                {content: "Quand tu t'aperçois qu'il y a 50 div fermé dans ton HTML"},
                {content: "Quand le client regarde tes Pull Request"},
                {content: "Quand le client regarde tes Pull Request"},
                {content: "Quand le client regarde tes Pull Request"},
                {content: "Quand le client regarde tes Pull Request"},
            ] %}
                            {% for message in messages %}
                                <div class="row">
                                    <div class="col-8">

                                        <p class="bg-white p-1 rounded">
                                            {{ message.content }}
                                        </p>
                                    </div>
                                    <div class="col-4">
                                        <button type="button" class="btn btn-danger">
                                            Delete
                                        </button>
                                    </div>
                                </div>
                            {% endfor %}
                            <div class="col text-center mt-5">
                                <button type="button" style="width: 10rem" class="btn btn-outline-danger bg-white btn-lg text-center">
                                    Delete All
                                </button>
                                <p class="mt-3 text-justify text-white">
                                    Supprime tout le MEME, légendes et images comprises.
                                </p>
                            </div>
                        </div>

                    </div>
                </div>
            {% endblock %}
